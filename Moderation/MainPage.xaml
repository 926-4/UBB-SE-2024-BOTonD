<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Moderation.MainPage">

    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25">
            <Image
                Source="dotnet_bot.png"
                HeightRequest="500"
                Aspect="AspectFit"
                SemanticProperties.Description="norbert pop" />

            <Label
                Text="Hello, World!"
                Style="{StaticResource Headline}"
                SemanticProperties.HeadingLevel="Level1" 
                x:Name="HelloLabel"/>
            <Label
                Text="Will get replaced"
                Style="{StaticResource Headline}"
                SemanticProperties.HeadingLevel="Level2" 
                x:Name="LastLoginLabel"/>

            <Button 
                    x:Name="JoinGroupButton"
                    Text="Join group" 
                    Clicked="OnJoinGroupClicked"
                    HorizontalOptions="Fill"/>
            <Button 
                    x:Name="LogOutButton"
                    Text="Log Out" 
                    Clicked="OnLogOutClicked"
                    HorizontalOptions="Fill"/>
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
